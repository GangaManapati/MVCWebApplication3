@model MVCWebApplication3.Models.Product

@{
    ViewData["Title"] = "GetProductById";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h1>GetProductById</h1>

<div>
    <h4>Product</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IsActive)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsActive)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="UpdateProduct" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="DeleteProduct" asp-route-id="@Model?.Id">Delete</a> |
    <a asp-action="ActivateProduct" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="DeactivateProduct" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="GetProducts">Back to List</a>
</div>
